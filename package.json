{
  "name": "jt-harness-plugin",
  "version": "2.0.0",
  "private": true,
  "description": "JurisLM's automatically invoked engineering rules for Codex.",
  "license": "UNLICENSED",
  "engines": {
    "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
  },
  "scripts": {
    "test": "node --test scripts/*.test.mjs",
    "manifest:check": "node scripts/validate-plugin-manifests.mjs",
    "lint:md": "markdownlint README.md 'skills/*/SKILL.md'",
    "validate": "npm run manifest:check && npm run test && npm run lint:md"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "markdownlint-cli": "^0.49.1"
  }
}
